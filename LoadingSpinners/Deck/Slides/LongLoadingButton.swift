import SwiftUI
import DeckUI
import ButtonKit

struct LongLoadingButton: View {
    var body: some View {
        VStack {
            AppStoreButton()
        }
    }
}

#Preview {
    SlidesPreview(slides: [])
}
